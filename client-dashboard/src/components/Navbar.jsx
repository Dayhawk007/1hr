import React from 'react';

function Navbar() {
  return (
    <div className="bg-gray-800 p-4 flex justify-between items-center">
      {/* Left side content */}
      {/* Right side content (e.g., user profile, notifications) */}
    </div>
  );
}

export default Navbar;
